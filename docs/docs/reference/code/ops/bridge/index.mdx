---
title: "ops/bridge/"
---

This directory contains operational config for the bridge service.

- `subject_map.yaml` defines canonical → legacy subject mapping rules consumed by the bridge (`bridge/mapping.go`).

