---
title: "ops/jetstream/"
---

This directory contains JetStream stream and consumer definitions used by local ops tooling and bootstrapping.

## What to read first

- `streams.dex.json`: canonical stream definition (subjects + retention)
- `consumer.swaps.json`: example consumer config for swaps
- `streams.legacy.json`: legacy stream used by the bridge during cutover

